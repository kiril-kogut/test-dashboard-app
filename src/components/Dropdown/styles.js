import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles({
  text: {
    fontFamily: 'Poppins',
    fontSize: '13px',
    fontWeight: '400',
    color: '#585858',
    textTransform: 'none',
  },
  icon: {
    color: '#bcbcbc',
  },
});

export default styles;
